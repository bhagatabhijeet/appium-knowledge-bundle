# Directory Update Log

## 2026-09-12
* **Creation**: Established the knowledge bundle scaffold — README, root index, and the Introduction topic covering what Appium is, its architecture, app types, advantages, tool comparison, and limitations.
* **Update**: Added the [Supported languages](/introduction/supported-languages.md) topic with a client-library diagram and reference table.
* **Update**: Enriched [Advantages](/introduction/advantages.md), [Appium compared with platform-specific tools](/introduction/tool-comparison.md), and [Limitations and considerations](/introduction/limitations.md) with detail from lecture transcripts (Selenium lineage, Sauce Labs backing, platform/unit/functional-testing criteria, local-lab scaling example, GitHub issues pointer).
* **Update**: Expanded [How Appium works: architecture overview](/introduction/architecture.md) with REST/session mechanics, the Selenium 3.x/4.x and Appium 1.x/2.x protocol history, the Java-client request-flow diagram, and the platform-drivers subsection (UiAutomator2, XCUITest, WebDriverAgent, Appium Settings, ADB).
* **Update**: Enriched [Types of mobile apps](/introduction/app-types.md) with development-cycle, speed, user-experience, and automation-difficulty notes for native, hybrid, and mobile web apps, plus a comparison diagram.
* **Update**: Added a banner image and the official Appium logo, and expanded the "what this bundle offers" description, in the root README.
* **Update**: Linked client library, driver, and documentation references to their verified sources under github.com/appium (and appium-boneyard for the archived PHP client) in [References](/introduction/references.md).
* **Restructure**: Split the single Introduction document into individual concept files under `introduction/`, each with `type` frontmatter, per the OKF v0.2 bundle structure (§3).
* **Creation**: Added the [Ways to set up Appium](/introduction/setup-options.md) topic (host OS vs. target platform) and the `driver-ecosystem/` folder ([The Appium ecosystem](/driver-ecosystem/overview.md), [How Appium drivers work](/driver-ecosystem/how-drivers-work.md), [Appium driver catalog](/driver-ecosystem/drivers.md)).
* **Creation**: Built `viz.html`, a self-contained interactive graph viewer for the bundle, generated by `tools/generate-viz.js` from the OKF reference viewer template.
* **Creation**: Added [UiAutomator2 vs. Espresso](/driver-ecosystem/uiautomator2-vs-espresso.md), extracted from `UiAutomator2VsEspresso.pptx` (deleted after extraction).
* **Update**: Added a "Reference links" section to the root README (Appium docs/GitHub/driver/client links, plus Cucumber references), verifying each URL and correcting two that had moved (iOS real-device setup, hybrid app automation).
* **Creation**: Added the `appium-setup/` folder with [Appium setup prerequisites](/appium-setup/prerequisites.md) — admin access, OS currency, office-machine antivirus/policy blockers, emulator/simulator resource needs, and OEM device restrictions (citing a real Appium GitHub issue on a Realme device) — from a lecture transcript plus Appium's official System Requirements doc. `windows/` and `mac/` subfolders are planned but not yet written.
* **Creation**: Added `appium-setup/windows/` with [Install Node.js and npm on Windows](/appium-setup/windows/install-nodejs.md), the first step of the Windows setup lecture (checking for an existing install, downloading the LTS build, running the installer). `mac/` is still not yet written.

## 2026-09-14
* **Creation**: Added [Install the Appium server (npm)](/appium-setup/windows/install-appium-server.md), the second step of the Windows setup — the `npm install -g appium@next` command and expected deprecation-warning output, a version check (`appium -v`) directly after install, finding the install location (`npm root -g` plus `where appium` for Command Prompt / `(Get-Command appium).Source` for PowerShell Core), checking installed drivers with real captured output (`appium driver list --installed`, `appium driver list`), and a "Confirm the driver installed" re-check step after `appium driver install`. Linked from `windows/index.md`, added to `generate-viz.js`'s `READING_ORDER`, `viz.html` regenerated.
* **Creation**: Added [Install Appium Inspector on Windows](/appium-setup/windows/install-appium-inspector.md), the third Windows setup step — downloading the desktop app, starting the Appium server, configuring and starting a session in Appium Inspector, and using a successful session to verify the whole server/driver/app setup end-to-end. Linked from `windows/index.md`, added to `generate-viz.js`'s `READING_ORDER`, `viz.html` regenerated.
* **Update**: Added a "Known warnings on first start" note to [Install Appium Inspector on Windows](/appium-setup/windows/install-appium-inspector.md)'s "Start the Appium server" section, covering the `DEP0190` Node deprecation notice and the `uiautomator2` peer-dependency warning seen against Appium 2.0.1, both non-blocking; regenerated `viz.html` to match.
* **Update**: Added real installer wizard screenshots (Choose Installation Options, Choose Install Location, Completing Setup — usernames redacted) to [Install Appium Inspector on Windows](/appium-setup/windows/install-appium-inspector.md)'s "Download and install" section, under `assets/images/install-appium-inspector-wizard-screen{1,2,3}.png`; regenerated `viz.html` to match.
* **Update**: Added a visual verification check to [Install the Appium server (npm)](/appium-setup/windows/install-appium-server.md)'s "Find where the server was installed" section — a File Explorer screenshot of `node_modules\appium\lib` (username redacted), confirming `main.js` and the rest of the server source files are present; saved as `assets/images/install-appium-server-lib-folder.png`, `viz.html` regenerated.
* **Update**: Verified and documented the actual fix for the `uiautomator2` peer-dependency warning in [Install Appium Inspector on Windows](/appium-setup/windows/install-appium-inspector.md)'s "Known warnings on first start" section — reinstalling the driver at `uiautomator2@4.2.9` plus upgrading the server to `appium@2.19.0` (the latest 2.x release) clears it; confirmed hands-on that both commands together remove the warning, `DEP0190` remains cosmetic and unfixable by version changes; `viz.html` regenerated.
* **Update**: Added an "Uninstall the server" section to [Install the Appium server (npm)](/appium-setup/windows/install-appium-server.md) — `npm uninstall -g appium`, a note that drivers/SDKs aren't removed with it, and confirming removal via `appium -v` failing; `viz.html` regenerated.
* **Update**: Added an "Explore the CLI" section to [Install the Appium server (npm)](/appium-setup/windows/install-appium-server.md) with real `appium --help` output, summarizing the four subcommands (`server`, `driver`, `plugin`, `setup`); `viz.html` regenerated.
* **Creation**: Added [Set up the Android SDK and JDK on Windows](/appium-setup/windows/setup-android-sdk.md), from a lecture transcript — system requirements, installing Android Studio, the first-run SDK Setup Wizard, confirming SDK Platforms/Tools components in the SDK Manager, setting `ANDROID_HOME`/`PATH`/`JAVA_HOME`, and verification with real captured `appium driver doctor uiautomator2` output (before/after `ANDROID_HOME` is set) from this machine. Linked from `windows/index.md`, added to `generate-viz.js`'s `READING_ORDER`, `viz.html` regenerated.
